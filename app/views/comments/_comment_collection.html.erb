<div id="<%= dom_id(blog_post, :comments) %>">
    <% blog_post.comments.where(parent: parent).order(created_at: :desc).each do |comment| %>
        <%= render "comments/comment", comment: comment, blog_post: blog_post %>
    <% end %>
</div>
